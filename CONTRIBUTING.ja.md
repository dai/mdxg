# MDXG へのコントリビュート

**Language:** [English](CONTRIBUTING.md) | 日本語

MDXG に関心を持っていただきありがとうございます。  
このプロジェクトはまだ初期段階で、仕様へのフィードバック、バグ報告、新しい実装アイデア、コード貢献のいずれも歓迎しています。

## コントリビュート方法

### 仕様へのフィードバック

[SPEC.md](SPEC.md)（日本語版: [SPEC.ja.md](SPEC.ja.md)）は継続的に更新される文書です。  
不明確・不足・誤りがある場合は Issue を作成してください。  
仕様に関する議論は、コードと同じくらい重要です。

### バグ報告

パーサー、VS Code 拡張、Web アプリでバグを見つけた場合は、次を含めて Issue を作成してください。

- 期待した挙動
- 実際に起きた挙動
- 再現できる最小の markdown ファイル

### 新しい実装の追加

MDXG は任意のプラットフォームで実装できるよう設計されています。新しい参考実装を追加する場合:

1. `packages/` 配下に新しいディレクトリを作成
2. 文書分割と見出し抽出に [`@mdxg/parser`](packages/parser) を使う（または同等ロジックを対象言語に移植）
3. 対象プラットフォーム向けに仕様を実装
4. [README.md](README.md) の Reference Implementations 表へ追加
5. Pull Request を作成

### 既存コードへの貢献

既存パッケージへの変更は次の手順で行ってください。

1. リポジトリを Fork
2. `main` からブランチを作成
3. 変更を実装
4. テスト実行: `pnpm test`
5. 変更内容と理由を明確に記載して PR を作成

## プロジェクト構成

```text
packages/parser   → @mdxg/parser, 共有 markdown パーサー（基盤）
packages/vscode   → @mdxg/vscode, VS Code 拡張
apps/web          → @mdxg/web, ドキュメントサイト（mdxg.org）
```

パーサーがコアライブラリで、VS Code 拡張と Web アプリはそれを土台にしたプラットフォーム実装です。

## 開発セットアップ

```bash
# 依存関係のインストール
pnpm install

# 全パッケージをビルド
pnpm build

# Web アプリを開発モードで起動
pnpm dev

# パーサーテストを実行
pnpm test
```

## 運用上のルール

- 仕様文書では RFC 2119 の語（MUST / SHOULD / MAY）を厳密に扱ってください。
- パーサーはフレームワーク非依存を維持してください（不要な DOM 依存や Node 固有依存を増やさない）。
- VS Code 拡張と Web アプリは参考実装です。仕様に実装を合わせるのが原則です。差異がある場合は、実装修正または仕様更新の議論を行ってください。

## ライセンス

コントリビュート時点で、あなたの貢献は [Apache 2.0 License](LICENSE) の下で提供されることに同意したものとします。
