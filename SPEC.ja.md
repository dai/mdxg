# Markdown Experience Guidelines

**Language:** [English](SPEC.md) | 日本語

**Version 0.1.0 (Draft)**

> **この仕様は作業中です。** セクション番号・要求事項・適合レベルは変更される可能性があります。  
> フィードバックは [GitHub リポジトリ](https://github.com/vercel-labs/mdxg) へお願いします。

この文書は **Markdown Experience Guidelines (MDXG)** の日本語版です。  
MDXG は、Markdown ドキュメントを提示・閲覧・編集するインターフェースが備えるべき能力を定義します。

MDXG は構文仕様ではありません。CommonMark / GFM / MDX を置き換えず、1 段上の体験レイヤーを定義します。

**規範語の扱い:** MUST / MUST NOT / SHOULD / SHOULD NOT / MAY は RFC 2119 に基づいて解釈してください。  
厳密な解釈が必要な場合は英語版（`SPEC.md`）を正とします。

## Motivation

Markdown は AI と人間の共通フォーマットですが、多くの UI は長文を平坦に表示するだけで、構造ナビゲーションや位置把握が弱いという課題があります。  
MDXG は、プラットフォーム固有の UI 設計自由度を保ちながら、共通して提供すべき体験能力を標準化します。

## Definitions

- **Document**: 単一の Markdown ファイル
- **Page**: 見出し境界で分割された仮想ページ
- **Page navigation**: ページ間を移動するための仕組み
- **Page outline**: 現在ページ内の見出し構造を示す仕組み
- **Preview mode**: レンダリング表示
- **Source view**: シンタックスハイライト付きの読み取り専用ソース表示
- **Source edit**: 編集可能なソース表示（変更は元文書へ同期）

## Rendering

### 1. Theming

適合実装はホスト環境のテーマへ追従しなければなりません。

- 背景・前景・アクセント・境界色はホストテーマ/OS 由来であるべき
- ユーザーに色設定を強制してはならない
- ライト/ダークの双方をサポートしなければならない
- MDXG は特定の配色・フォント・見た目を規定しない

### 2. Code Block Rendering

- 言語識別子付きフェンスコードは適切にシンタックスハイライトされなければならない
- 言語未指定コードも等幅の整形テキストで表示されなければならない
- 各コードブロックにワンアクションでコピーできる UI が必要
- ハイライト配色はホストのライト/ダークに追従すること

### 3. Task Lists

- `- [ ]` は未チェック、`- [x]` はチェック済みとして表示すること
- インタラクティブ可否は実装判断（読み取り専用でもよい）
- リスト構造とインデントは保持すること

### 4. Images

- 画像はプレビュー内でインライン表示されること
- 相対パスは文書位置基準で解決すること
- 画像はコンテンツ幅内に収まり、横方向にはみ出さないこと
- 代替テキストを保持し、支援技術からアクセス可能にすること
- 画像読込失敗時は alt 表示が推奨

### 5. Tables

- テーブルのセル区切りは視認可能であること
- ヘッダー行は本文行と区別されること
- Markdown の列揃え指定を尊重すること
- 横幅超過時は横スクロール可能にし、レイアウト破壊を防ぐこと

## Document Structure

### 6. Virtual Pages

#### 6.1 Page Boundaries

- H1/H2（ATX と setext の両形式）境界で仮想ページ分割すること
- フェンスコード内の見出しはページ境界として扱ってはならない
- ページ境界見出しはページタイトルになる

#### 6.2 Pre-heading Content

- 最初の H1/H2 前の内容は、必要に応じて "Introduction" ページとして扱うべき
- 空白のみなら Introduction ページを作らないべき

#### 6.3 Page Depth

- H1 は depth-1、H2 は depth-2
- 一覧表示時に階層が分かる表現を行うべき

#### 6.4 Page Identifiers

- 各ページは URL-safe な一意 slug を持つこと
- 重複 slug は数値サフィックス等で衝突回避すること

### 7. Page Navigation

適合実装は次を提供しなければなりません。

- 全ページを順序付きで見られる
- 任意ページへ移動できる
- 現在ページが識別できる
- 前後ページへ順次移動できる

### 8. Page Outline

- 現在ページ内の H3–H6 を一覧し、見出しへ移動できること
- 見出し深さは視覚的に伝えるべき
- 現在表示中見出しの示唆（scroll-spy 等）が推奨
- H3–H6 がないページでは空/非表示でもよい

### 9. Sequential Navigation

- previous/next ページへ移動する手段を必須提供
- 前後ページタイトル表示を推奨
- 適用不可な操作（最初の previous 等）は非表示または無効化
- 少なくとも 1 箇所でアクセス可能であること

### 10. Search

- 全ページ横断検索手段を必須提供
- 検索対象は「ユーザーに見えるレンダリングテキスト」
- 現在一致箇所をハイライトし、次/前へ移動可能であること
- ページを跨いでも一致位置を保持して遷移すること
- 件数表示は推奨

## Editing

### 11. Mode Toggle

適合実装は少なくとも次のモードを切替可能にすること。

#### 11.1 Preview Mode

- アクティブページのみのレンダリング表示
- ページタイトルを明確に表示
- 標準 Markdown 要素を適切に描画
- H3–H6 に安定したリンク可能 ID を付与

#### 11.2 Source View

- 文書全体の生 Markdown を表示
- 見出し・装飾・コード・リンク等のシンタックスハイライト推奨
- 読み取り専用でもよい

#### 11.3 Source Edit（MDXG Editor 必須）

- 編集可能であること
- 編集結果が基底文書へ同期されること

#### 11.4 Toggle Behavior

- 切替は同一画面内で行う（新規タブ/ウィンドウを開かない）
- 現在モードが明示されること
- ソースモードでナビゲーション要素を表示するかは実装判断

### 12. Document Links

- 他 Markdown へのリンクは、同じ MDXG 体験で開くことが推奨
- フラグメント付きリンクは対象見出し/ページへ遷移推奨
- ファイル未存在時はエラー表示し、無言失敗しないこと
- 相対リンクは現在文書基準で解決
- 外部 URL はホスト既定動作でよい

## Accessibility

### 13. Keyboard Navigation

コアナビゲーションはキーボードで操作可能でなければなりません。

- ページ一覧を矢印キーで操作可能
- Enter 等でページ選択遷移できる
- ページ遷移時に適切なフォーカス管理を行うべき
- 前後遷移のキーボード操作を推奨
- すべての操作要素にアクセシブル名を持たせること

## Extensions（SHOULD）

### 14. Math Rendering

- `$...$` / `$$...$$` の数式描画を推奨
- 未対応時は生構文を保全し、欠落/破損させないこと

### 15. Diagram Rendering

- ` ```mermaid ` を最低限サポート推奨
- 未対応言語は通常コードブロックとしてフォールバック必須

### 16. Footnotes

- `[^1]` 参照と定義を相互リンク表示することを推奨
- 未対応時も生構文を保全すること

## Conformance

### MDXG Viewer

読み取り専用実装。必須:

- Section 1–10, 13

### MDXG Editor

Viewer のすべて + 必須:

- Section 11.3 / 11.4
- Section 12

Sections 14–16（拡張）は SHOULD であり、適合判定の必須要件ではありません。
